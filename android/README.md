# Android Transport Stubs

This directory contains the transport adapter interfaces for the ResilientPay Android app.

**NOTE (Blocker):** Java and `kotlinc` were not found on the build system. The stubs have been written but the Gradle build could not be verified locally. Tests are pending Java environment setup.
